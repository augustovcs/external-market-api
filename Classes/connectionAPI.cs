using System.Net;
using System.Text.Json;

namespace api_external_scrapper.Classes;

public class connectionAPI
{
    public static void ConnectionAPI()
    {
        string QUERY_URL = "";
        Uri queryURI = new Uri(QUERY_URL);

        WebClient client = new WebClient();

    }
}